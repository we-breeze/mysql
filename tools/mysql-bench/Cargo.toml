[package]
name = "mysql-bench"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
brz-mysql = { path = "../.." }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync", "time"] }
